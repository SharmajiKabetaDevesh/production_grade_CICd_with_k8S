apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: learnk8scsi-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: game.bestdeveshkrsharma.tech
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: learnk8scsi-service
            port:
              number: 80
